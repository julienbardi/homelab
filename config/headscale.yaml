# ============================================================
# headscale.yaml
# ------------------------------------------------------------
# Headscale configuration template
# ============================================================

server_url: http://10.89.12.4:8080
listen_addr: 0.0.0.0:8080
private_key_path: /etc/headscale/private.key

noise:
  private_key_path: /etc/headscale/noise_private.key

database:
  type: sqlite3
  sqlite_path: /etc/headscale/db.sqlite

prefixes:
  v4: 100.100.0.0/24       # VPN client subnet
  v6: fd7a:115c:a1e0::/48  # Example IPv6 pool (required even if unused)
  allocation: sequential   # or "random"

dns:
  override_local_dns: true
  base_domain: tailnet
  nameservers:
    global:
      - 10.89.12.4   # CoreDNS on NAS
      - 1.1.1.1      # Cloudflare DNS as fallback

