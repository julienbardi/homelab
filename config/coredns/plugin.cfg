# Directives are registered in the order they should be executed.
# Ordering is VERY important. Every plugin will feel the effects of all other
# plugin below (after) them during a request, but they must not care what plugin
# above them are doing.
#
# How to rebuild with updated plugin configurations: Modify the list below and
# run `go generate && go build`
#
# Format:
#   <plugin-name>:<package-name>
# Example:
#   log:log
#
# External plugin example:
#   log:github.com/coredns/coredns/plugin/log
#
# https://coredns.io/manual/plugins/?utm_source=copilot.com

# --- Curated active plugins ---
errors:errors
health:health
ready:ready
log:log
reload:reload
root:root

cache:cache
dnssec:dnssec
forward:forward
doh:github.com/coredns/doh
hosts:hosts
file:file
auto:auto
template:template

prometheus:metrics
whoami:whoami

# --- Full default plugin list (commented for reference) ---
# root:root
# metadata:metadata
# geoip:geoip
# cancel:cancel
# tls:tls
# quic:quic
# timeouts:timeouts
# multisocket:multisocket
# reload:reload
# nsid:nsid
# bufsize:bufsize
# bind:bind
# debug:debug
# trace:trace
# ready:ready
# health:health
# pprof:pprof
# prometheus:metrics
# errors:errors
# log:log
# dnstap:dnstap
# local:local
# dns64:dns64
# acl:acl
# any:any
# chaos:chaos
# loadbalance:loadbalance
# tsig:tsig
# cache:cache
# rewrite:rewrite
# header:header
# dnssec:dnssec
# autopath:autopath
# minimal:minimal
# template:template
# transfer:transfer
# hosts:hosts
# route53:route53
# azure:azure
# clouddns:clouddns
# k8s_external:k8s_external
# kubernetes:kubernetes
# file:file
# auto:auto
# secondary:secondary
# etcd:etcd
# loop:loop
# forward:forward
# grpc:grpc
# erratic:erratic
# whoami:whoami
# on:github.com/coredns/caddy/onevent
# sign:sign
# view:view
# nomad:nomad
