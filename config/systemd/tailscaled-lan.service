# /etc/systemd/system/tailscaled-lan.service
[Unit]
Description=Tailscale LAN role (managed by homelab Makefile)
After=tailscaled.service
Requires=tailscaled.service

[Service]
Type=oneshot
ExecStart=/bin/true
RemainAfterExit=yes

[Install]
WantedBy=multi-user.target
